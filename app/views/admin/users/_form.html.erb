<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :user_email, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :roles %>
    <%= form.select(:roles, @roles.collect {|r| [r.name, r.id] },
                    { prompt: "Please select" },
                    { multiple: true, selected: @user.roles.include?(r) }
                   ) %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<%# button_to "delete", admin_user_path(user), method: :delete, data: { confirm: "You sure?" } %>
