<div class="comments">
  <% comments.each do |comment| %>
    <div class="comment">
      <p><%= comment.user.username %></p>
      <p><%= comment.created_at %></p>
      <p><%= comment.body %></p>
    </div>
  <% end %>
</div>
